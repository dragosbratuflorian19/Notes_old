Neural networks:
 - Convolutional neural networks: good for image recognition
 - Long short-term memory network: good for speech recognition
 - plain vanilla  aka multilayer perceptron
 - Relu: rectified linear unit: RElu(a) = max(0, a)
 - Sigmoid: Slow learner

 Anaconda distribution:
 - Install pytorch:
	conda install pytorch torchvision -c pytorch
 - Check if pytorch is installed:
	conda list pytorch

